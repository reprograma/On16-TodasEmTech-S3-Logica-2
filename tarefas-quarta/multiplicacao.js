//Crie uma função que recebe 2 parâmetros 
//e retorna a multiplicação entre eles.

function multiplica(num1, num2) {
  let resultado = num1 * num2
  return `O resultado da multiplicação é ${resultado}.`
}

console.log(multiplica(4, 2))
